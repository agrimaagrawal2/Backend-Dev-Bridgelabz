// app.js
const { logActivity } = require("./logger");

// Simulate user flow
logActivity("User 'Amit' logged in.");
logActivity("User searched for 'Nodejs books'.");
logActivity("User logged out.");